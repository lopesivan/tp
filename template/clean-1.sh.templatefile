#!/bin/bash
# ----------------------------------------------------------------------------
A=.befor.action
B=.after.action

test ! -f $A && { find . | grep -v "${0}" > $A ; exit 1; }

find . > $B

(grep -f $B $A ; cat $B) | sort | uniq -u | sed 's/^/rm -rf /'
# ----------------------------------------------------------------------------
exit 0
