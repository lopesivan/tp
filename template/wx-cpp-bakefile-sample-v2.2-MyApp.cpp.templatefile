#include "MyApp.h"
#include "MyFrame.h"

bool MyApp::OnInit()
{

	MyFrame *frame = new MyFrame(wxT("The Main Frame"));
	frame->Show(true);
	/*
		wxMessageBox(
		    wxT("Hello	wxCpp"),
		    wxT("wxApp"),
		    wxOK | wxICON_INFORMATION
		);
	*/

	return true;
}
